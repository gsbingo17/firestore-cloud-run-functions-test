{
  "name": "firestore-first-function",
  "version": "1.0.0",
  "description": "First function that triggers Firestore document changes",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=modifyDocument --source=src/firstFunction.js --port=8080 --signature-type=http --security-level=none"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "firebase-admin": "^11.11.0"
  }
}
